import type { TierId, WaveDefinition } from '../types';
export declare function waveClearGoldBonus(waveNumber: number): number;
export declare function getWaveCount(): number;
export declare function getWave(areaId: string, tierId: TierId, waveIndex: number): WaveDefinition;
